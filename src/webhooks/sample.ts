import { DynamicServiceBody, ActionReturnValue, SandboxLib } from 'wordparrot-types'

export const sampleWebhook = async (body: DynamicServiceBody, lib: SandboxLib): ActionReturnValue => {
    return {}
}