<!-- 
    Insert any files you'd like to make available to the client within this folder. 

    These files should also have metadata included in the "attachments" array of src/config/plugin.ts.

    Without this metadata, the files are not discoverable.
-->